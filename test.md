我在实现一个固件仿真工具，功能如下：

0. 使用qemu-user来进行单服务仿真
1. 使用lua脚本来实现系统调用劫持，当qemu启动时，会用参数来指定存放lua脚本的路径。当执行系统调用时，会进入到 linux-user/syscall.c 的 do_syscall函数
2. 在这个函数会调用指定lua脚本来进行系统调用的劫持，如果存在同名的文件，比如open系统调用对应着open.lua，就会执行open.lua的逻辑

目前已经实现了部分逻辑，但是我希望你能帮我增加其他的c函数接口，可以使得lua脚本可以直接调用


1. guest内存与host内存相互转变的函数
2. 修改指定寄存器值的函数
